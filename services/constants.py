"""
ScanStruct 全局常量

集中管理跨模块使用的魔法数字：页面尺寸、DPI、阈值等。
"""
from __future__ import annotations

# =============================================================================
# 页面尺寸（像素 @ 300 DPI）
# =============================================================================

# A4 纸 @ 300 DPI
DEFAULT_PAGE_WIDTH = 2480   # 210mm → ~2480px
DEFAULT_PAGE_HEIGHT = 3508  # 297mm → ~3508px

# =============================================================================
# DPI 设置
# =============================================================================

DEFAULT_DPI = 200  # 处理用 DPI（OCR 识别 200 DPI 足够，减少图片体积和传输时间）

# =============================================================================
# 标题检测阈值
# =============================================================================

FONT_SIZE_RATIO_THRESHOLD = 1.3  # 标题字体 > 中位数 × 此值 → 提升级别
CENTER_TOLERANCE = 0.10          # 横向居中容忍范围（页面宽度比例）
TOP_PAGE_RATIO = 0.05            # 页面顶部区域比例

# =============================================================================
# 文本 PDF 检测阈值
# =============================================================================

# 每页最少字符数才算"可提取文字页"（排除仅含页眉页脚/水印的页面）
MIN_CHARS_PER_TEXT_PAGE: int = 300
